# Конфигурация для SQL Server ARSEN
# Windows Authentication (как показано в SSMS)

PORT=3000
NODE_ENV=development

# SQL Server подключение
DB_HOST=ARSEN
DB_PORT=1433
DB_NAME=dentreserve_pro
DB_TRUSTED_CONNECTION=true
DB_ENCRYPT=true
DB_TRUST_CERT=true

# Если используете SQL Server Authentication вместо Windows Auth:
# DB_TRUSTED_CONNECTION=false
# DB_USER=sa
# DB_PASSWORD=ваш_пароль

# JWT секретный ключ (минимум 32 символа)
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа_для_jwt_токенов
JWT_EXPIRES_IN=7d

# Разрешенные источники для CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
